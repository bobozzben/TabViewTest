# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

source 'https://github.com/CocoaPods/Specs.git'

target 'TabViewTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TabViewTest
  # 1090925 
  pod 'Firebase/Crashlytics'
  pod 'Firebase/Analytics'
  pod 'Firebase/Messaging'

 # old
 # pod 'Firebase/Core'
 # pod 'Firebase/Auth'
 # pod 'Firebase/Database'
 # pod 'Firebase/Messaging'
 # pod 'Firebase/Storage'
 # pod 'GoogleMaps'
 # pod 'GooglePlaces'

  # The platform of the target `TabViewTest` (iOS 9.0) is not compatible with `DuoAPISwift (2.0.0)`, which does not support `iOS`.
  #pod 'DuoAPISwift', '~> 2.0.1'


  target 'TabViewTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TabViewTestUITests' do
    inherit! :search_paths
    # Pods for testing
  end


end
